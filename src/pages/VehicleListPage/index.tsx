import { VehiclesList } from 'entities/vehicle';

export const VehicleListPage = () => {
    return (
        <div>
            <h1>Vehicles</h1>
            <VehiclesList />
        </div>
    );
};
